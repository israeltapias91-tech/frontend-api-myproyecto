server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Esta es la línea mágica que arregla el error 404
        try_files $uri $uri/ /index.html;
    }
}